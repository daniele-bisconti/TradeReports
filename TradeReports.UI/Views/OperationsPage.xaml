<Page
    x:Class="TradeReports.UI.Views.OperationsPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:properties="clr-namespace:TradeReports.UI.Properties"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Button
            Command="{Binding AddOperation}">
            Aggiungi Operazione
        </Button>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
            Style="{StaticResource PageTitleStyle}"
            Margin="{StaticResource MediumLeftMargin}"
            Text="{x:Static properties:Resources.OperationsPageTitle}" />
            <Grid Grid.Row="1">
                <DataGrid
                AutoGenerateColumns="False"
                GridLinesVisibility="Horizontal"
                CanUserAddRows="False"
                ItemsSource="{Binding Source}"
                Margin="10,10,14,-10"
                KeyboardNavigation.TabNavigation="Once">
                    <DataGrid.Columns>
                        <!--
                        TODO WTS:
                        Remove this column definitions and define columns for your data.
                        Consider adding header properties to Resources.resw
                    -->
                        <DataGridTextColumn Binding="{Binding Id}" Header="OpID" />
                        <DataGridTextColumn Binding="{Binding OpenDate}" Header="OpenDate" />
                        <DataGridTextColumn Binding="{Binding CloseDate}" Header="CloseDate" />
                        <DataGridTextColumn Binding="{Binding CloseDate}" Header="CloseDate" />
                        <DataGridTextColumn Binding="{Binding Day}" Header="Day" />
                        <DataGridTextColumn Binding="{Binding GapHour}" Header="Gap H" />
                        <DataGridTextColumn Binding="{Binding TradeNumber}" Header="N. Trade Tot." />
                        <DataGridTextColumn Binding="{Binding MonthTradeNumber}" Header="N. Trade M." />
                        <DataGridTextColumn Binding="{Binding CapitalAT}" Header="CapitalAT" />
                        <DataGridTextColumn Binding="{Binding CapitalDT}" Header="CapitalDT" />
                        <DataGridTextColumn Binding="{Binding PL}" Header="PL" />
                        <DataGridTextColumn Binding="{Binding Pos.Description}" Header="Pos" />
                        <DataGridTextColumn Binding="{Binding Size}" Header="Size" />
                        <DataGridTextColumn Binding="{Binding Category.Description}" Header="Categoria" />
                        <DataGridTextColumn Binding="{Binding Tool.Description}" Header="Strumento" />
                        <DataGridTextColumn Binding="{Binding Note}" Header="Note" />

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </StackPanel>
</Page>
